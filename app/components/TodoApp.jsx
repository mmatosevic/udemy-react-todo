var React = require('react');

var TodoApp = React.createClass({
    render: () => {
        return (
            <div>
                <h1>ToDo App</h1>
            </div>
        )
    }
});

module.exports = TodoApp;